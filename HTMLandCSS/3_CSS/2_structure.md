# Структура CSS

Основная стркуктура:
```CSS
selector {
    rule
}
```

Селектор указывает какие элементы на странице нужно выбрать.
Правило имеент вид: `свойство: значение`.

```CSS
selector1,
selector2 {/* cooment */
    property: value;
}
```

